# This file exists so pytest can differentiate between unit tests and
# integration tests. Integration tests (test_api_compatibility.py, etc.)
# are meant to be run manually against a live service, not in CI.
